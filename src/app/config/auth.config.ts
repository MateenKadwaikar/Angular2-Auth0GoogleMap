interface AuthConfiguration {
    clientID: string;
    domain: string;
    googleApi: string;
}

export const myConfig: AuthConfiguration = {
    clientID: '',
    domain: '',
    googleApi : '',
};
